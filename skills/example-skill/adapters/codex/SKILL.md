---
name: example-skill
description: Use this adapter when loading the example skill template in Codex or copying it as the starting point for a Codex skill.
---

# Codex Adapter

Read the tool-agnostic core instructions in `../../SKILL.md` before acting.

When adapting this template for Codex:

1. Keep `name` lowercase with hyphen-separated words.
2. Rewrite `description` so it clearly states when Codex should use the skill.
3. Keep detailed workflow, safety notes, references, and scripts in the core
   skill directory unless Codex needs a tool-specific override.
